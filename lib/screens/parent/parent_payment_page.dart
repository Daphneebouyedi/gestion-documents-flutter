import 'package:flutter/material.dart';

class ParentPaymentPage extends StatelessWidget {
  const ParentPaymentPage({super.key});

  @override
  Widget build(BuildContext context) {
    const Color primaryColor = Color(0xFF24B6AA);

    return Scaffold(
// Fichier supprimé